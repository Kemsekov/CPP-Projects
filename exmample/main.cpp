#include <iostream>
#include <fstream>
#include <cctype>
#include <vector>
#include <math.h>
#include <filesystem>
#include <algorithm>
template<typename iterator>
void quickSort(iterator begin, iterator end){
    
}

int main(){
    using namespace std;
    vector<int> vec{};
    for(int i = 0;i<100;i++){
        vec.push_back(rand());
    }
    return 0;   
}
